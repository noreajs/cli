/**
 * {{name}} middleware
 */
class {{name}}Middleware {
  /**
   * Only administrator can access to the resource
   *
   * @param req
   * @param res
   * @param next
   */
  yourMethod(req, res, next) {
    // you code here
    return next();
  }
}

export default new {{name}}Middleware();