{{#if mongoose}}
import { Document } from "mongoose";

export default interface {{name}} extends Document {
{{else}}
export default interface {{name}} {
{{/if}}
    // properties
    {{#if timestamps}}
    createdAt: Date;
    updatedAt: Date;
    {{/if}}
    {{#if softDelete}}
    deletedAt?: Date;
    {{/if}}
}